<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wkr.document.mapper.DocumentMapper">

    <update id="updateProcessSuccess">
        update document_info
        set status = 2, error_message = null, update_time = now()
        where id = #{id}
        and status = 1
    </update>

    <update id="updateProcessFailed">
        update document_info
        set status = 3, error_message = #{errorMessage}, update_time = now()
        where id = #{id}
        and status = 1
    </update>
</mapper>