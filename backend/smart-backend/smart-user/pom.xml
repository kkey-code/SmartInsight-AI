<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.smart.ai</groupId>
        <artifactId>smart-backend</artifactId>
        <version>1.0.0</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

    <artifactId>smart-user</artifactId>
    <name>smart-user</name>

    <dependencies>
        <!-- ==================== Common 模块（拆分后） ==================== -->
        <!-- 核心工具（JWT、工具类、常量等） -->
        <dependency>
            <groupId>com.smart.ai</groupId>
            <artifactId>smart-common-core</artifactId>
        </dependency>

        <!-- Web 配置（Knife4j、全局异常等） -->
        <dependency>
            <groupId>com.smart.ai</groupId>
            <artifactId>smart-common-web</artifactId>
        </dependency>

        <!-- 数据库配置（MyBatis Plus） -->
        <dependency>
            <groupId>com.smart.ai</groupId>
            <artifactId>smart-common-db</artifactId>
        </dependency>

        <!-- ==================== API 模块（自己的 API，暴露给其他服务） ==================== -->
        <dependency>
            <groupId>com.smart.ai</groupId>
            <artifactId>smart-api-user</artifactId>
        </dependency>

        <!-- ==================== Spring Boot ==================== -->
        <!-- Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- 参数校验 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <!-- 管理端点 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <!-- ==================== Spring Cloud ==================== -->
        <!-- Nacos 服务注册发现 -->
        <dependency>
            <groupId>com.alibaba.cloud</groupId>
            <artifactId>spring-cloud-starter-alibaba-nacos-discovery</artifactId>
        </dependency>

        <!-- ==================== 数据库 ==================== -->
        <!-- MySQL 驱动（smart-common-db 已包含，但为了显式声明也可以保留） -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- ==================== 工具 ==================== -->
        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <scope>provided</scope>
        </dependency>

        <!-- ==================== Spring Security（密码加密） ==================== -->
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-crypto</artifactId>
        </dependency>

        <!-- ==================== 测试 ==================== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>